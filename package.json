{
  "name": "budget-lab-charts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Budget Lab chart archive — versioned chart specs + data, built and published by budget-lab-chart-engine.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-all.mjs",
    "build": "node scripts/build-all.mjs",
    "catalog": "node scripts/build-catalog.mjs",
    "site": "node scripts/assemble-site.mjs",
    "thumbs": "node scripts/thumbs.mjs",
    "all": "npm run validate && npm run build && npm run catalog && npm run site && npm run thumbs"
  },
  "dependencies": {
    "budget-lab-chart-engine": "github:Budget-Lab-Yale/budget-lab-chart-engine#v1.0.0"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
